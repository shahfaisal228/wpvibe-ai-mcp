{
  "ok": true,
  "id": "ac-read-inc-body",
  "action": "read_file",
  "result": {
    "path": "inc/homeglo-manager-portal.php",
    "content": "\t\t\t)\n\t\t);\n\n\t\tif ( $staff && password_verify( $password, $staff->password_hash ) ) {\n\t\t\tdelete_transient( $key );\n\t\t\thomeglo_manager_log( 'staff_login', 'Signed in to HomeGlo management.', 'staff', $staff->id, $staff );\n\t\t\thomeglo_manager_set_session( $staff->id );\n\t\t\twp_safe_redirect( homeglo_manager_url() );\n\t\t\texit;\n\t\t}\n\n\t\t$customer = wp_authenticate( $identifier, $password );\n\t\tif ( ! is_wp_error( $customer ) && $customer instanceof WP_User && in_array( 'customer', (array) $customer->roles, true ) ) {\n\t\t\tdelete_transient( $key );\n\t\t\twp_set_current_user( $customer->ID );\n\t\t\twp_set_auth_cookie( $customer->ID, true, is_ssl() );\n\t\t\t$my_account = function_exists( 'wc_get_page_permalink' ) ? wc_get_page_permalink( 'myaccount' ) : home_url( '/my-account/' );\n\t\t\twp_safe_redirect( $my_account );\n\t\t\texit;\n\t\t}\n\n\t\tset_transient( $key, $attempts + 1, 15 * MINUTE_IN_SECONDS );\n\t\twp_safe_redirect( homeglo_manager_url( 'login', array( 'error' => 'invalid' ) ) );\n\t\texit;\n\t}\n\n\tif ( ! homeglo_manager_logged_in() && ! in_array( $view, array( 'login', 'setup', 'forgot-password', 'reset-password' ), true ) ) {\n\t\twp_safe_redirect( homeglo_manager_url( 'login' ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && 'login' === $view ) {\n\t\twp_safe_redirect( homeglo_manager_url() );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && in_array( $view, array( 'admin', 'logs' ), true ) && ! homeglo_manager_is_owner() ) {\n\t\thomeglo_manager_log( 'access_denied', 'Attempted to open owner-only area: ' . $view, 'portal', 0 );\n\t\twp_safe_redirect( homeglo_manager_url( 'dashboard', array( 'denied' => '1' ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && ! homeglo_manager_is_owner() ) {\n\t\t$required_permission = homeglo_manager_view_permission( $view );\n\t\tif ( $required_permission && ! homeglo_manager_can( $required_permission ) ) {\n\t\t\thomeglo_manager_log( 'access_denied', 'Permission denied for: ' . $required_permission, 'portal', 0 );\n\t\t\twp_safe_redirect( homeglo_manager_url( 'dashboard', array( 'denied' => '1' ) ) );\n\t\t\texit;\n\t\t}\n\t}\n\n\tif ( homeglo_manager_logged_in() && 'add-product' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && 'create_product' === sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ) ) {\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'create_product', $csrf ) ) {\n\t\t\twp_die( 'Security check failed.' );\n\t\t}\n\n\t\tif ( ! class_exists( 'WC_Product_Simple' ) ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'woocommerce' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\t$name = sanitize_text_field( wp_unslash( $_POST['product_name'] ?? '' ) );\n\t\t$product_type = 'variable' === sanitize_key( wp_unslash( $_POST['product_type'] ?? 'simple' ) ) ? 'variable' : 'simple';\n\t\t$sku = sanitize_text_field( wp_unslash( $_POST['product_sku'] ?? '' ) );\n\t\t$regular_raw = wc_clean( wp_unslash( $_POST['regular_price'] ?? '' ) );\n\t\t$sale_raw = wc_clean( wp_unslash( $_POST['sale_price'] ?? '' ) );\n\t\t$stock_raw = wc_clean( wp_unslash( $_POST['stock_quantity'] ?? '' ) );\n\t\t$category_id = absint( $_POST['product_category'] ?? 0 );\n\t\t$status = 'publish' === sanitize_key( wp_unslash( $_POST['save_mode'] ?? 'draft' ) ) ? 'publish' : 'draft';\n\t\t$short_description = wp_kses_post( wp_unslash( $_POST['short_description'] ?? '' ) );\n\t\t$full_description = wp_kses_post( wp_unslash( $_POST['full_description'] ?? '' ) );\n\t\t$stock_status = 'outofstock' === sanitize_key( wp_unslash( $_POST['stock_status'] ?? 'instock' ) ) ? 'outofstock' : 'instock';\n\t\t$supplier_name = sanitize_text_field( wp_unslash( $_POST['supplier_name'] ?? '' ) );\n\t\t$supplier_sku = sanitize_text_field( wp_unslash( $_POST['supplier_sku'] ?? '' ) );\n\t\t$supplier_url = esc_url_raw( wp_unslash( $_POST['supplier_url'] ?? '' ) );\n\t\t$supplier_cost = wc_clean( wp_unslash( $_POST['supplier_cost'] ?? '' ) );\n\t\t$supplier_shipping = wc_clean( wp_unslash( $_POST['supplier_shipping_cost'] ?? '' ) );\n\t\t$shipping_origin = sanitize_text_field( wp_unslash( $_POST['shipping_origin'] ?? '' ) );\n\t\t$processing_days = absint( $_POST['processing_days'] ?? 0 );\n\t\t$delivery_min = absint( $_POST['delivery_min_days'] ?? 0 );\n\t\t$delivery_max = absint( $_POST['delivery_max_days'] ?? 0 );\n\n\t\tif ( ! $name ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'name' ) ) );\n\t\t\texit;\n\t\t}\n\t\tif ( '' === $regular_raw || ! is_numeric( $regular_raw ) || (float) $regular_raw < 0 ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'price' ) ) );\n\t\t\texit;\n\t\t}\n\t\tif ( '' !== $sale_raw && ( ! is_numeric( $sale_raw ) || (float) $sale_raw < 0 || (float) $sale_raw >= (float) $regular_raw ) ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'sale' ) ) );\n\t\t\texit;\n\t\t}\n\t\tif ( $sku && wc_get_product_id_by_sku( $sku ) ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'sku' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\t$variation_attributes = array();\n\t\tif ( 'variable' === $product_type && function_exists( 'homeglo_management_variation_attributes' ) && function_exists( 'homeglo_management_parse_options' ) ) {\n\t\t\tforeach ( homeglo_management_variation_attributes() as $slug => $label ) {\n\t\t\t\t$options = homeglo_management_parse_options( wp_unslash( $_POST[ 'attribute_' . $slug ] ?? '' ) );\n\t\t\t\tif ( $options ) {\n\t\t\t\t\t$variation_attributes[ $slug ] = array( 'label' => $label, 'options' => $options );\n\t\t\t\t}\n\t\t\t}\n\t\t\tif ( ! $variation_attributes ) {\n\t\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'attributes' ) ) );\n\t\t\t\texit;\n\t\t\t}\n\t\t}\n\n\t\t$image_ids = array();\n\t\tif ( ! empty( $_FILES['product_images']['name'] ) && is_array( $_FILES['product_images']['name'] ) ) {\n\t\t\trequire_once ABSPATH . 'wp-admin/includes/file.php';\n\t\t\trequire_once ABSPATH . 'wp-admin/includes/media.php';\n\t\t\trequire_once ABSPATH . 'wp-admin/includes/image.php';\n\t\t\t$original_files = $_FILES;\n\t\t\t$count = min( 6, count( $_FILES['product_images']['name'] ) );\n\t\t\tfor ( $i = 0; $i < $count; $i++ ) {\n\t\t\t\tif ( empty( $_FILES['product_images']['name'][ $i ] ) ) { continue; }\n\t\t\t\t$_FILES['homeglo_product_image_single'] = array(\n\t\t\t\t\t'name'     => $_FILES['product_images']['name'][ $i ],\n\t\t\t\t\t'type'     => $_FILES['product_images']['type'][ $i ],\n\t\t\t\t\t'tmp_name' => $_FILES['product_images']['tmp_name'][ $i ],\n\t\t\t\t\t'error'    => $_FILES['product_images']['error'][ $i ],\n\t\t\t\t\t'size'     => $_FILES['product_images']['size'][ $i ],\n\t\t\t\t);\n\t\t\t\t$attachment_id = media_handle_upload( 'homeglo_product_image_single', 0 );\n\t\t\t\tif ( ! is_wp_error( $attachment_id ) ) { $image_ids[] = absint( $attachment_id ); }\n\t\t\t}\n\t\t\t$_FILES = $original_files;\n\t\t}\n\n\t\ttry {\n\t\t\t$product = 'variable' === $product_type ? new WC_Product_Variable() : new WC_Product_Simple();\n\t\t\t$product->set_name( $name );\n\t\t\t$product->set_status( $status );\n\t\t\tif ( $sku ) { $product->set_sku( $sku ); }\n\t\t\t$product->set_short_description( $short_description );\n\t\t\t$product->set_description( $full_description );\n\t\t\tif ( $category_id ) { $product->set_category_ids( array( $category_id ) ); }\n\t\t\tif ( $image_ids ) {\n\t\t\t\t$product->set_image_id( $image_ids[0] );\n\t\t\t\tif ( count( $image_ids ) > 1 ) { $product->set_gallery_image_ids( array_slice( $image_ids, 1 ) ); }\n\t\t\t}\n\n\t\t\tif ( 'simple' === $product_type ) {\n\t\t\t\t$product->set_regular_price( wc_format_decimal( $regular_raw ) );\n\t\t\t\tif ( '' !== $sale_raw ) { $product->set_sale_price( wc_format_decimal( $sale_raw ) ); }\n\t\t\t\tif ( '' !== $stock_raw && is_numeric( $stock_raw ) ) {\n\t\t\t\t\t$product->set_manage_stock( true );\n\t\t\t\t\t$product->set_stock_quantity( max( 0, (int) $stock_raw ) );\n\t\t\t\t\t$product->set_stock_status( (int) $stock_raw > 0 ? 'instock' : 'outofstock' );\n\t\t\t\t} else {\n\t\t\t\t\t$product->set_manage_stock( false );\n\t\t\t\t\t$product->set_stock_status( $stock_status );\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\t$wc_attributes = array();\n\t\t\t\t$position = 0;\n\t\t\t\tforeach ( $variation_attributes as $slug => $attribute_data ) {\n\t\t\t\t\t$attribute = new WC_Product_Attribute();\n\t\t\t\t\t$attribute->set_id( 0 );\n\t\t\t\t\t$attribute->set_name( $attribute_data['label'] );\n\t\t\t\t\t$attribute->set_options( $attribute_data['options'] );\n\t\t\t\t\t$attribute->set_position( $position++ );\n\t\t\t\t\t$attribute->set_visible( true );\n\t\t\t\t\t$attribute->set_variation( true );\n\t\t\t\t\t$wc_attributes[] = $attribute;\n\t\t\t\t}\n\t\t\t\t$product->set_attributes( $wc_attributes );\n\t\t\t\t$product->set_stock_status( 'instock' );\n\t\t\t}\n\n\t\t\t$product_id = $product->save();\n\n\t\t\tif ( 'variable' === $product_type && $product_id && function_exists( 'homeglo_management_build_combinations' ) ) {\n\t\t\t\tforeach ( homeglo_management_build_combinations( $variation_attributes, 60 ) as $combo ) {\n\t\t\t\t\t$variation = new WC_Product_Variation();\n\t\t\t\t\t$variation->set_parent_id( $product_id );\n\t\t\t\t\t$attrs = array();\n\t\t\t\t\tforeach ( $combo as $slug => $value ) {\n\t\t\t\t\t\t$attrs[ sanitize_title( $variation_attributes[ $slug ]['label'] ) ] = $value;\n\t\t\t\t\t}\n\t\t\t\t\t$variation->set_attributes( $attrs );\n\t\t\t\t\t$variation->set_regular_price( wc_format_decimal( $regular_raw ) );\n\t\t\t\t\tif ( '' !== $sale_raw ) { $variation->set_sale_price( wc_format_decimal( $sale_raw ) ); }\n\t\t\t\t\tif ( '' !== $stock_raw && is_numeric( $stock_raw ) ) {\n\t\t\t\t\t\t$variation->set_manage_stock( true );\n\t\t\t\t\t\t$variation->set_stock_quantity( max( 0, (int) $stock_raw ) );\n\t\t\t\t\t\t$variation->set_stock_status( (int) $stock_raw > 0 ? 'instock' : 'outofstock' );\n\t\t\t\t\t}\n\t\t\t\t\t$variation->set_status( 'publish' );\n\t\t\t\t\t$variation->save();\n\t\t\t\t}\n\t\t\t\tWC_Product_Variable::sync( $product_id );\n\t\t\t\twc_delete_product_transients( $product_id );\n\t\t\t}\n\n\t\t\tif ( $product_id ) {\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_supplier_name', $supplier_name );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_supplier_sku', $supplier_sku );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_supplier_url', $supplier_url );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_supplier_cost', '' !== $supplier_cost && is_numeric( $supplier_cost ) ? wc_format_decimal( $supplier_cost ) : '' );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_supplier_shipping_cost', '' !== $supplier_shipping && is_numeric( $supplier_shipping ) ? wc_format_decimal( $supplier_shipping ) : '' );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_shipping_origin', $shipping_origin );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_processing_days', $processing_days );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_delivery_min_days', $delivery_min );\n\t\t\t\tupdate_post_meta( $product_id, '_homeglo_delivery_max_days', $delivery_max );\n\t\t\t\tforeach ( $image_ids as $image_id ) {\n\t\t\t\t\tif ( ! get_post_meta( $image_id, '_wp_attachment_image_alt', true ) ) {\n\t\t\t\t\t\tupdate_post_meta( $image_id, '_wp_attachment_image_alt', $name );\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t}\n\t\t} catch ( Exception $e ) {\n\t\t\t$product_id = 0;\n\t\t}\n\n\t\tif ( empty( $product_id ) ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'add-product', array( 'error' => 'save' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\thomeglo_manager_log( 'product_created', 'Created product: ' . $name . ' (' . $status . ')', 'product', absint( $product_id ) );\n\t\t$product_return_view = homeglo_manager_can( 'products' ) ? 'products' : 'dashboard';\n\t\twp_safe_redirect( homeglo_manager_url( $product_return_view, array( 'created' => absint( $product_id ) ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && 'profile' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && 'save_profile' === sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ) ) {\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'save_profile', $csrf ) ) {\n\t\t\twp_die( 'Security check failed.' );\n\t\t}\n\n\t\t$manager = homeglo_manager_current();\n\t\tif ( ! $manager ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'login' ) );\n\t\t\texit;\n\t\t}\n\n\t\t$display = sanitize_text_field( wp_unslash( $_POST['profile_display_name'] ?? '' ) );\n\t\t$username = sanitize_user( wp_unslash( $_POST['profile_username'] ?? '' ), true );\n\t\t$email = sanitize_email( wp_unslash( $_POST['profile_email'] ?? '' ) );\n\t\t$current_password = (string) wp_unslash( $_POST['current_password'] ?? '' );\n\t\t$new_password = (string) wp_unslash( $_POST['new_password'] ?? '' );\n\t\t$confirm_password = (string) wp_unslash( $_POST['confirm_password'] ?? '' );\n\n\t\tif ( ! $username || ! is_email( $email ) ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'profile', array( 'error' => 'details' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\tglobal $wpdb;\n\t\t$table = homeglo_manager_table();\n\t\t$duplicate = $wpdb->get_var(\n\t\t\t$wpdb->prepare(\n\t\t\t\t\"SELECT id FROM {$table} WHERE id <> %d AND (username = %s OR email = %s) LIMIT 1\",\n\t\t\t\tabsint( $manager->id ),\n\t\t\t\t$username,\n\t\t\t\t$email\n\t\t\t)\n\t\t);\n\t\tif ( $duplicate ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'profile', array( 'error' => 'duplicate' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\t$update = array(\n\t\t\t'display_name' => $display ? $display : $username,\n\t\t\t'username'     => $username,\n\t\t\t'email'        => $email,\n\t\t\t'updated_at'   => current_time( 'mysql', true ),\n\t\t);\n\t\t$formats = array( '%s', '%s', '%s', '%s' );\n\n\t\tif ( $new_password || $confirm_password || $current_password ) {\n\t\t\tif ( ! password_verify( $current_password, (string) $manager->password_hash ) ) {\n\t\t\t\twp_safe_redirect( homeglo_manager_url( 'profile', array( 'error' => 'current-password' ) ) );\n\t\t\t\texit;\n\t\t\t}\n\t\t\tif ( strlen( $new_password ) < 12 || ! hash_equals( $new_password, $confirm_password ) ) {\n\t\t\t\twp_safe_redirect( homeglo_manager_url( 'profile', array( 'error' => 'password' ) ) );\n\t\t\t\texit;\n\t\t\t}\n\t\t\t$update['password_hash'] = password_hash( $new_password, PASSWORD_DEFAULT );\n\t\t\t$formats[] = '%s';\n\t\t}\n\n\t\t$saved = $wpdb->update(\n\t\t\t$table,\n\t\t\t$update,\n\t\t\tarray( 'id' => absint( $manager->id ) ),\n\t\t\t$formats,\n\t\t\tarray( '%d' )\n\t\t);\n\n\t\tif ( false === $saved ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'profile', array( 'error' => 'save' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\thomeglo_manager_log( 'profile_updated', 'Updated own HomeGlo profile.', 'staff', absint( $manager->id ) );\n\t\thomeglo_manager_log( 'profile_updated', 'Updated own HomeGlo profile.', 'staff', absint( $manager->id ) );\n\t\twp_safe_redirect( homeglo_manager_url( 'profile', array( 'saved' => '1' ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && homeglo_manager_is_owner() && 'admin' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && 'save_staff_access' === sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ) ) {\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'manage_staff', $csrf ) ) { wp_die( 'Security check failed.' ); }\n\n\t\t$current = homeglo_manager_current();\n\t\t$target_id = absint( $_POST['staff_id'] ?? 0 );\n\t\tif ( ! $current || ! $target_id || absint( $current->id ) === $target_id ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_error' => 'self' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\tglobal $wpdb;\n\t\t$table = homeglo_manager_table();\n\t\t$target = $wpdb->get_row( $wpdb->prepare( \"SELECT * FROM {$table} WHERE id = %d LIMIT 1\", $target_id ) );\n\t\tif ( ! $target ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_error' => 'missing' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\t$new_role = sanitize_key( wp_unslash( $_POST['staff_role'] ?? 'manager' ) );\n\t\tif ( ! in_array( $new_role, array( 'owner', 'manager' ), true ) ) { $new_role = 'manager'; }\n\n\t\tif ( 'owner' === sanitize_key( $target->role ) && 'manager' === $new_role && (int) $target->active === 1 ) {\n\t\t\t$other_active_owners = (int) $wpdb->get_var(\n\t\t\t\t$wpdb->prepare( \"SELECT COUNT(*) FROM {$table} WHERE role = 'owner' AND active = 1 AND id <> %d\", $target_id )\n\t\t\t);\n\t\t\tif ( $other_active_owners < 1 ) {\n\t\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_error' => 'last-owner' ) ) );\n\t\t\t\texit;\n\t\t\t}\n\t\t}\n\n\t\t$allowed_keys = array_keys( homeglo_manager_permission_definitions() );\n\t\t$posted_permissions = isset( $_POST['staff_permissions'] ) && is_array( $_POST['staff_permissions'] ) ? array_map( 'sanitize_key', wp_unslash( $_POST['staff_permissions'] ) ) : array();\n\t\t$permissions = array_values( array_intersect( $posted_permissions, $allowed_keys ) );\n\t\t$permission_json = 'manager' === $new_role ? wp_json_encode( $permissions ) : '';\n\n\t\t$wpdb->update(\n\t\t\t$table,\n\t\t\tarray(\n\t\t\t\t'role'            => $new_role,\n\t\t\t\t'permissions'     => $permission_json,\n\t\t\t\t'session_hash'    => '',\n\t\t\t\t'session_expires' => null,\n\t\t\t\t'updated_at'      => current_time( 'mysql', true ),\n\t\t\t),\n\t\t\tarray( 'id' => $target_id ),\n\t\t\tarray( '%s', '%s', '%s', '%s', '%s' ),\n\t\t\tarray( '%d' )\n\t\t);\n\n\t\t$labels = array();\n\t\tforeach ( $permissions as $permission ) {\n\t\t\t$definitions = homeglo_manager_permission_definitions();\n\t\t\tif ( isset( $definitions[ $permission ] ) ) { $labels[] = $definitions[ $permission ]; }\n\t\t}\n\t\t$detail = sprintf(\n\t\t\t'Changed %s to %s. Permissions: %s',\n\t\t\t$target->display_name ? $target->display_name : $target->username,\n\t\t\t'owner' === $new_role ? 'Admin / Owner' : 'Manager',\n\t\t\t'owner' === $new_role ? 'Full access' : ( $labels ? implode( ', ', $labels ) : 'No operational features' )\n\t\t);\n\t\thomeglo_manager_log( 'staff_access_updated', $detail, 'staff', $target_id );\n\n\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_updated' => 'access' ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && homeglo_manager_is_owner() && 'admin' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && in_array( sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ), array( 'block_staff', 'unblock_staff', 'delete_staff' ), true ) ) {\n\t\t$action = sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) );\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'manage_staff', $csrf ) ) { wp_die( 'Security check failed.' ); }\n\n\t\t$current = homeglo_manager_current();\n\t\t$target_id = absint( $_POST['staff_id'] ?? 0 );\n\t\tif ( ! $current || ! $target_id || absint( $current->id ) === $target_id ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_error' => 'self' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\thomeglo_manager_ensure_table();\n\t\tglobal $wpdb;\n\t\t$table = homeglo_manager_table();\n\t\t$target = $wpdb->get_row( $wpdb->prepare( \"SELECT * FROM {$table} WHERE id = %d LIMIT 1\", $target_id ) );\n\t\tif ( ! $target ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_error' => 'missing' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\tif ( 'owner' === sanitize_key( $target->role ) && in_array( $action, array( 'block_staff', 'delete_staff' ), true ) ) {\n\t\t\t$other_active_owners = (int) $wpdb->get_var(\n\t\t\t\t$wpdb->prepare( \"SELECT COUNT(*) FROM {$table} WHERE role = 'owner' AND active = 1 AND id <> %d\", $target_id )\n\t\t\t);\n\t\t\tif ( $other_active_owners < 1 ) {\n\t\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_error' => 'last-owner' ) ) );\n\t\t\t\texit;\n\t\t\t}\n\t\t}\n\n\t\t$target_label = $target->display_name ? $target->display_name : $target->username;\n\t\tif ( 'block_staff' === $action ) {\n\t\t\t$wpdb->update(\n\t\t\t\t$table,\n\t\t\t\tarray( 'active' => 0, 'session_hash' => '', 'session_expires' => null, 'updated_at' => current_time( 'mysql', true ) ),\n\t\t\t\tarray( 'id' => $target_id ),\n\t\t\t\tarray( '%d', '%s', '%s', '%s' ),\n\t\t\t\tarray( '%d' )\n\t\t\t);\n\t\t} elseif ( 'unblock_staff' === $action ) {\n\t\t\t$wpdb->update(\n\t\t\t\t$table,\n\t\t\t\tarray( 'active' => 1, 'session_hash' => '', 'session_expires' => null, 'updated_at' => current_time( 'mysql', true ) ),\n\t\t\t\tarray( 'id' => $target_id ),\n\t\t\t\tarray( '%d', '%s', '%s', '%s' ),\n\t\t\t\tarray( '%d' )\n\t\t\t);\n\t\t} elseif ( 'delete_staff' === $action ) {\n\t\t\t$wpdb->delete( $table, array( 'id' => $target_id ), array( '%d' ) );\n\t\t}\n\n\t\t$staff_log_action = 'delete_staff' === $action ? 'staff_removed' : ( 'block_staff' === $action ? 'staff_blocked' : 'staff_unblocked' );\n\t\thomeglo_manager_log( $staff_log_action, ucfirst( str_replace( '_', ' ', $action ) ) . ': ' . $target_label, 'staff', $target_id );\n\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'staff_updated' => $action ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && homeglo_manager_is_owner() && 'customers' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && in_array( sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ), array( 'block_customer', 'unblock_customer' ), true ) ) {\n\t\t$action = sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) );\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'manage_customers', $csrf ) ) { wp_die( 'Security check failed.' ); }\n\n\t\t$customer_id = absint( $_POST['customer_id'] ?? 0 );\n\t\t$customer = $customer_id ? get_userdata( $customer_id ) : false;\n\t\tif ( ! $customer || ! in_array( 'customer', (array) $customer->roles, true ) ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'customers', array( 'customer_error' => 'missing' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\tif ( 'block_customer' === $action ) {\n\t\t\tupdate_user_meta( $customer_id, '_homeglo_account_blocked', '1' );\n\t\t\tif ( class_exists( 'WP_Session_Tokens' ) ) {\n\t\t\t\tWP_Session_Tokens::get_instance( $customer_id )->destroy_all();\n\t\t\t}\n\t\t} else {\n\t\t\tdelete_user_meta( $customer_id, '_homeglo_account_blocked' );\n\t\t}\n\n\t\thomeglo_manager_log( 'block_customer' === $action ? 'customer_blocked' : 'customer_unblocked', ( 'block_customer' === $action ? 'Blocked customer: ' : 'Unblocked customer: ' ) . $customer->user_email, 'customer', $customer_id );\n\t\twp_safe_redirect( homeglo_manager_url( 'customers', array( 'customer_updated' => $action ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && homeglo_manager_is_owner() && 'admin' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && 'create_staff' === sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ) ) {\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'create_staff', $csrf ) ) { wp_die( 'Security check failed.' ); }\n\n\t\thomeglo_manager_ensure_table();\n\t\tglobal $wpdb;\n\t\t$table = homeglo_manager_table();\n\t\t$username = sanitize_user( wp_unslash( $_POST['staff_username'] ?? '' ), true );\n\t\t$email = sanitize_email( wp_unslash( $_POST['staff_email'] ?? '' ) );\n\t\t$display = sanitize_text_field( wp_unslash( $_POST['staff_display_name'] ?? '' ) );\n\t\t$password = (string) wp_unslash( $_POST['staff_password'] ?? '' );\n\t\t$role = sanitize_key( wp_unslash( $_POST['staff_role'] ?? 'manager' ) );\n\t\tif ( ! in_array( $role, array( 'owner', 'manager' ), true ) ) { $role = 'manager'; }\n\t\t$permission_keys = array_keys( homeglo_manager_permission_definitions() );\n\t\t$posted_permissions = isset( $_POST['staff_permissions'] ) && is_array( $_POST['staff_permissions'] ) ? array_map( 'sanitize_key', wp_unslash( $_POST['staff_permissions'] ) ) : array();\n\t\t$initial_permissions = 'manager' === $role\n\t\t\t? ( ! empty( $_POST['permissions_configured'] ) ? array_values( array_intersect( $posted_permissions, $permission_keys ) ) : homeglo_manager_default_permissions() )\n\t\t\t: array();\n\t\t$initial_permissions_json = 'manager' === $role ? wp_json_encode( $initial_permissions ) : '';\n\n\t\tif ( ! $username || ! is_email( $email ) || strlen( $password ) < 12 ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'error' => 'details' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\t$duplicate = $wpdb->get_var( $wpdb->prepare( \"SELECT id FROM {$table} WHERE username = %s OR email = %s LIMIT 1\", $username, $email ) );\n\t\tif ( $duplicate ) {\n\t\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'error' => 'duplicate' ) ) );\n\t\t\texit;\n\t\t}\n\n\t\t$now = current_time( 'mysql', true );\n\t\t$wpdb->insert(\n\t\t\t$table,\n\t\t\tarray(\n\t\t\t\t'username'      => $username,\n\t\t\t\t'email'         => $email,\n\t\t\t\t'display_name'  => $display ? $display : ( 'owner' === $role ? 'HomeGlo Admin' : 'HomeGlo Manager' ),\n\t\t\t\t'role'          => $role,\n\t\t\t\t'permissions'   => $initial_permissions_json,\n\t\t\t\t'password_hash' => password_hash( $password, PASSWORD_DEFAULT ),\n\t\t\t\t'active'        => 1,\n\t\t\t\t'created_at'    => $now,\n\t\t\t\t'updated_at'    => $now,\n\t\t\t),\n\t\t\tarray( '%s', '%s', '%s', '%s', '%s', '%s', '%d', '%s', '%s' )\n\t\t);\n\n\t\t$created_staff_id = absint( $wpdb->insert_id );\n\t\tif ( $created_staff_id ) {\n\t\t\t$permission_labels = array();\n\t\t\t$definitions = homeglo_manager_permission_definitions();\n\t\t\tforeach ( $initial_permissions as $permission ) {\n\t\t\t\tif ( isset( $definitions[ $permission ] ) ) { $permission_labels[] = $definitions[ $permission ]; }\n\t\t\t}\n\t\t\thomeglo_manager_log(\n\t\t\t\t'staff_created',\n\t\t\t\t'Created ' . ( 'owner' === $role ? 'Admin / Owner' : 'Manager' ) . ': ' . ( $display ? $display : $username ) . '. Access: ' . ( 'owner' === $role ? 'Full access' : ( $permission_labels ? implode( ', ', $permission_labels ) : 'No operational features' ) ),\n\t\t\t\t'staff',\n\t\t\t\t$created_staff_id\n\t\t\t);\n\t\t}\n\t\t$welcome_email_sent = false;\n\t\tif ( $created_staff_id ) {\n\t\t\t$login_url = homeglo_manager_url( 'login' );\n\t\t\t$reset_url = homeglo_manager_url( 'forgot-password' );\n\t\t\t$role_label = 'owner' === $role ? 'Admin / Owner' : 'Manager';\n\t\t\t$subject = 'Welcome to HomeGlo — your ' . $role_label . ' account';\n\t\t\t$body = '<div style=\"font-family:Arial,sans-serif;max-width:620px;margin:auto;color:#2b2622\">'\n\t\t\t\t. '<h2 style=\"margin-bottom:8px\">Welcome to HomeGlo</h2>'\n\t\t\t\t. '<p>Your HomeGlo ' . esc_html( $role_label ) . ' account has been created.</p>'\n\t\t\t\t. '<p><strong>Username:</strong> ' . esc_html( $username ) . '<br><strong>Role:</strong> ' . esc_html( $role_label ) . '</p>'\n\t\t\t\t. '<p style=\"margin:24px 0\"><a href=\"' . esc_url( $login_url ) . '\" style=\"display:inline-block;background:#2b2622;color:#fff;text-decoration:none;padding:12px 18px;border-radius:8px\">Sign in to HomeGlo</a></p>'\n\t\t\t\t. '<p>If you do not know your password or want to choose a new one, use the secure password reset page:</p>'\n\t\t\t\t. '<p><a href=\"' . esc_url( $reset_url ) . '\">' . esc_html( $reset_url ) . '</a></p>'\n\t\t\t\t. '<p style=\"color:#7d746a;font-size:12px;margin-top:28px\">HomeGlo staff access is separate from WordPress admin.</p>'\n\t\t\t\t. '</div>';\n\t\t\t$welcome_email_sent = (bool) wp_mail( $email, $subject, $body, array( 'Content-Type: text/html; charset=UTF-8' ) );\n\t\t}\n\n\t\twp_safe_redirect( homeglo_manager_url( 'admin', array( 'created' => '1', 'welcome_email' => $welcome_email_sent ? 'sent' : 'failed' ) ) );\n\t\texit;\n\t}\n\n\tif ( homeglo_manager_logged_in() && 'orders' === $view && 'POST' === strtoupper( $_SERVER['REQUEST_METHOD'] ?? 'GET' ) && 'save_fulfilment' === sanitize_key( wp_unslash( $_POST['manager_action'] ?? '' ) ) ) {\n\t\t$csrf = (string) wp_unslash( $_POST['csrf'] ?? '' );\n\t\tif ( ! homeglo_manager_verify_csrf( 'save_fulfilment', $csrf ) ) {\n\t\t\twp_die( 'Security check failed.' );\n\t\t}\n\n\t\t$order_id = absint( $_POST['order_id'] ?? 0 );\n\t\t$group_key = sanitize_key( wp_unslash( $_POST['group_key'] ?? '' ) );\n\t\t$order = wc_get_order( $order_id );\n\t\tif ( ! $order || ! function_exists( 'homeglo_fulfilment_order_groups' ) ) {\n\t\t\twp_die( 'Order not found.' );\n\t\t}\n\t\t$groups = homeglo_fulfilment_order_groups( $order );\n\t\tif ( ! isset( $groups[ $group_key ] ) ) { wp_die( 'Supplier group not found.' ); }\n\n\t\t$statuses = function_exists( 'homeglo_fulfilment_statuses' ) ? homeglo_fulfilment_statuses() : array();\n\t\t$status = sanitize_key( wp_unslash( $_POST['fulfilment_status'] ?? 'not_ordered' ) );\n\t\tif ( ! isset( $statuses[ $status ] ) ) { $status = 'not_ordered'; }\n\n\t\t$new_tracking = sanitize_text_field( wp_unslash( $_POST['tracking_number'] ?? '' ) );\n\t\t$new_tracking_url = esc_url_raw( wp_unslash( $_POST['tracking_url'] ?? '' ) );\n\t\t$quick_status = sanitize_key( wp_unslash( $_POST['quick_status'] ?? '' ) );\n\t\tif ( 'ordered' === $quick_status ) {\n\t\t\t$status = 'ordered';\n\t\t} elseif ( 'shipped' === $quick_status && $new_tracking ) {\n\t\t\t$status = 'shipped';\n\t\t}\n\n\t\t$saved = $order->get_meta( '_homeglo_fulfilment_groups', true );\n\t\t$saved = is_array( $saved ) ? $saved : array();\n\t\t$previous_status = (string) ( $groups[ $group_key ]['status'] ?? 'not_ordered' );\n\t\t$previous_tracking = (string) ( $groups[ $group_key ]['tracking_number'] ?? '' );\n\n\t\t$saved[ $group_key ] = array(\n\t\t\t'status'            => $status,\n\t\t\t'supplier_order_id' => sanitize_text_field( wp_unslash( $_POST['supplier_order_id'] ?? '' ) ),\n\t\t\t'tracking_number'   => $new_tracking,\n\t\t\t'tracking_url'      => $new_tracking_url,",
    "start_line": 580,
    "end_line": 1165,
    "total_lines": 1573,
    "scope": "draft",
    "theme": "homeglo-atelier-wpvibe-draft",
    "editable": true
  },
  "completed_at": "2026-09-20T17:57:49.628Z",
  "bridge_http_status": 200
}
